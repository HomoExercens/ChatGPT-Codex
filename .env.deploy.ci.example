# CI deploy smoke env (safe dummy values)
#
# Used by GitHub Actions to boot `docker-compose.deploy.yml` without secrets.
# It intentionally uses plain HTTP on localhost (no ACME / TLS).

# Reverse proxy front (Caddy)
DEPLOY_DOMAIN=http://localhost
NEUROLEAGUE_PUBLIC_BASE_URL=http://localhost

# Reverse proxy safety
NEUROLEAGUE_TRUST_PROXY_HEADERS=true
NEUROLEAGUE_ALLOWED_HOSTS=localhost,127.0.0.1
NEUROLEAGUE_CORS_ALLOWED_ORIGINS=http://localhost

# Auth
NEUROLEAGUE_AUTH_JWT_SECRET=ci-secret-change-me
NEUROLEAGUE_ADMIN_TOKEN=ci-admin-token

# Database (Postgres)
POSTGRES_USER=neuroleague
POSTGRES_PASSWORD=neuroleague
POSTGRES_DB=neuroleague

# Storage (MinIO / S3-compatible)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio12345
MINIO_BUCKET=neuroleague

# Optional: Caddy ACME email (unused in CI)
CADDY_EMAIL=

# Optional: reduce background work in CI
NEUROLEAGUE_LOG_JSON=false
NEUROLEAGUE_SCHEDULER_ENABLED=false
NEUROLEAGUE_BACKUP_ENABLED=false

# Seed demo content on boot (idempotent)
NEUROLEAGUE_SEED_ON_BOOT=1

